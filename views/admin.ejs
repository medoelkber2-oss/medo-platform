<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo&display=swap" rel="stylesheet">
    <style>body { font-family: 'Cairo'; background: #f8f9fa; }</style>
</head>
<body class="p-4">
    <div class="container bg-white p-4 shadow rounded-4">
        <div class="d-flex justify-content-between mb-4">
            <h2 class="fw-bold text-primary">لوحة إدارة الطلاب 🛠️</h2>
            <a href="/home" class="btn btn-outline-dark">الرجوع للمنصة</a>
        </div>
        
        <table class="table table-hover mt-3 align-middle">
            <thead class="table-dark">
                <tr><th>الاسم</th><th>الإيميل</th><th>تفعيل الكود</th><th>الجهاز</th><th>الإجراءات</th></tr>
            </thead>
            <tbody>
                <% students.forEach(s => { %>
                <tr>
                    <td><%= s.username %></td>
                    <td><%= s.email %></td>
                    <td><span class="badge <%= s.is_active ? 'bg-success' : 'bg-danger' %>"><%= s.is_active ? 'مفعل' : 'غير مفعل' %></span></td>
                    <td><%= s.device_info ? '✅ مربوط' : '⚠️ متاح' %></td>
                    <td>
                        <a href="/admin/reset/<%= s._id %>" class="btn btn-sm btn-warning">تصفير جهاز</a>
                        <a href="/admin/delete/<%= s._id %>" class="btn btn-sm btn-danger" onclick="return confirm('حذف نهائي؟')">حذف طالب</a>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>
