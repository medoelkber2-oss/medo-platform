<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة تحكم الأدمن 🛠️</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Cairo', sans-serif; background: #f8f9fa; padding: 20px; }
        .card { border-radius: 15px; border: none; }
    </style>
</head>
<body>
    <div class="container bg-white p-4 shadow rounded-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="text-primary fw-bold">لوحة تحكم مستر ميدو 🛠️</h2>
            <a href="/logout" class="btn btn-danger btn-sm">تسجيل خروج</a>
        </div>

        <% if (success) { %>
        <div class="alert alert-success text-center"><%= success %></div>
        <% } %>
        <% if (error) { %>
        <div class="alert alert-danger text-center"><%= error %></div>
        <% } %>

        <div class="row">
            <!-- إضافة كورس جديد -->
            <div class="col-md-6">
                <div class="card p-3 mb-4 shadow-sm border-primary">
                    <h5 class="fw-bold">إضافة كورس جديد 🎥</h5>
                    <form action="/admin/add-course" method="POST">
                        <div class="mb-2">
                            <input type="text" name="title" class="form-control" placeholder="اسم الكورس" required>
                        </div>
                        <div class="mb-2">
                            <input type="text" name="vid" class="form-control" placeholder="ID يوتيوب (مثال: dQw4w9W)" required>
                        </div>
                        <div class="mb-2">
                            <input type="text" name="thumb" class="form-control" placeholder="رابط صورة الغلاف" required>
                        </div>
                        <button type="submit" class="btn btn-primary w-100">إضافة الكورس</button>
                    </form>
                </div>
            </div>

            <!-- إضافة كود تفعيل -->
            <div class="col-md-6">
                <div class="card p-3 mb-4 shadow-sm border-warning">
                    <h5 class="fw-bold">إضافة كود تفعيل 🔑</h5>
                    <form action="/admin/add-code" method="POST">
                        <div class="mb-2">
                            <input type="text" name="newCode" class="form-control" placeholder="الكود (مثال: ABC123)" required>
                        </div>
                        <div class="mb-2">
                            <select name="courseId" class="form-control" required>
                                <option value="">اختر الكورس</option>
                                <% courses.forEach(c => { %>
                                    <option value="<%= c.id %>"><%= c.title %></option>
                                <% }) %>
                            </select>
                        </div>
                        <button type="submit" class="btn btn-warning w-100">إضافة الكود</button>
                    </form>
                    
                    <hr>
                    <a href="/admin/generate-keys" class="btn btn-outline-secondary btn-sm w-100">
                        🎲 توليد 20 كود عشوائي
                    </a>
                </div>
            </div>
        </div>

        <hr>

        <!-- إدارة الطلاب -->
        <h5 class="fw-bold mt-4">إدارة الطلاب 👥</h5>
        <div class="table-responsive">
            <table class="table table-hover align-middle mt-3">
                <thead class="table-dark">
                    <tr>
                        <th>الاسم</th>
                        <th>الإيميل</th>
                        <th>الكورسات</th>
                        <th>الإجراءات</th>
                    </tr>
                </thead>
                <tbody>
                    <% students.forEach(s => { %>
                    <tr>
                        <td><%= s.username %></td>
                        <td><%= s.email %></td>
                        <td>
                            <% 
                            let enrolled = {};
                            try { enrolled = JSON.parse(s.courses || '{}'); } catch(e) {}
                            %>
                            <span class="badge <%= Object.keys(enrolled).length > 0 ? 'bg-success' : 'bg-secondary' %>">
                                <%= Object.keys(enrolled).length %> كورس
                            </span>
                        </td>
                        <td>
                            <a href="/admin/delete-student/<%= s._id %>" class="btn btn-outline-danger btn-sm" onclick="return confirm('هل أنت متأكد؟')">حذف</a>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>

        <!-- الأكواد -->
        <h5 class="fw-bold mt-5">الأكواد المتوفرة 🔑</h5>
        <div class="d-flex flex-wrap gap-2">
            <% codes.forEach(c => { %>
                <span class="badge <%= c.used ? 'bg-secondary text-decoration-line-through' : 'bg-info' %> p-2">
                    <%= c.code %> - <%= c.course_id %>
                </span>
            <% }) %>
        </div>
    </div>
</body>
</html>
