<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Cairo', sans-serif; background: #f8f9fa; padding: 20px; }</style>
</head>
<body>
    <div class="container bg-white p-4 shadow rounded-4">
        <h2 class="text-primary fw-bold mb-4">لوحة تحكم مستر ميدو 🛠️</h2>

        <div class="row">
            <div class="col-md-7">
                <div class="card p-3 mb-4 shadow-sm">
                    <h5 class="fw-bold">إضافة فيديو جديد 🎥</h5>
                    <form action="/admin/add-course" method="POST" class="row g-2">
                        <div class="col-md-6"><input type="text" name="title" class="form-control" placeholder="اسم الدرس" required></div>
                        <div class="col-md-6"><input type="text" name="vid" class="form-control" placeholder="ID يوتيوب" required></div>
                        <div class="col-md-10"><input type="text" name="thumb" class="form-control" placeholder="رابط صورة الغلاف" required></div>
                        <div class="col-md-2"><button class="btn btn-primary w-100">إضافة</button></div>
                    </form>
                </div>
            </div>

            <div class="col-md-5">
                <div class="card p-3 mb-4 shadow-sm border-warning">
                    <h5 class="fw-bold">توليد كود تفعيل 🔑</h5>
                    <form action="/admin/add-code" method="POST" class="d-flex gap-2">
                        <input type="text" name="newCode" class="form-control" placeholder="اكتب الكود هنا" required>
                        <button class="btn btn-warning">حفظ</button>
                    </form>
                </div>
            </div>
        </div>

        <hr>

        <h5 class="fw-bold mt-4">قائمة الطلاب والبيانات المسجلة 👥</h5>
        <table class="table table-hover align-middle mt-3">
            <thead class="table-dark">
                <tr><th>الاسم</th><th>الإيميل</th><th>التفعيل</th><th>الجهاز</th><th>الإجراءات</th></tr>
            </thead>
            <tbody>
                <% students.forEach(s => { %>
                <tr>
                    <td><%= s.username %></td>
                    <td><%= s.email %></td>
                    <td><span class="badge <%= s.is_active ? 'bg-success' : 'bg-danger' %>"><%= s.is_active ? 'مفعل' : 'غير مفعل' %></span></td>
                    <td><%= s.device_info ? '✅ مربوط' : '⚠️ متاح' %></td>
                    <td>
                        <a href="/admin/reset-student/<%= s._id %>" class="btn btn-outline-warning btn-sm">تصفير الجهاز</a>
                        <a href="/admin/delete-student/<%= s._id %>" class="btn btn-outline-danger btn-sm" onclick="return confirm('حذف نهائي؟')">حذف الطالب</a>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>

        <h5 class="fw-bold mt-5">الأكواد المتاحة في القاعدة 🔑</h5>
        <div class="d-flex flex-wrap gap-2">
            <% codes.forEach(c => { %>
                <span class="badge <%= c.isUsed ? 'bg-secondary text-decoration-line-through' : 'bg-info' %> p-2">
                    <%= c.codeText %>
                </span>
            <% }) %>
        </div>
    </div>
</body>
</html>
