<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <title>لوحة تحكم ميدو 🛡️</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
</head>
<body class="bg-light p-5">
    <div class="container bg-white p-4 shadow rounded">
        <h2 class="text-primary mb-4">أهلاً يا أدمن ميدو 🛠️</h2>
        <div class="row">
            <div class="col-md-4">
                <h5>توليد أكواد</h5>
                <form action="/admin/generate" method="POST">
                    <select name="courseId" class="form-select mb-2">
                        <option value="c1">كورس البرمجة</option>
                        <option value="c2">احتراف التسويق</option>
                        <option value="c3">كورس ميدو الجديد</option>
                    </select>
                    <input type="number" name="count" class="form-control mb-2" placeholder="عدد الأكواد">
                    <button class="btn btn-success w-100">توليد</button>
                </form>
            </div>
            <div class="col-md-8">
                <h5>الطلاب المشتركين</h5>
                <table class="table">
                    <thead><tr><th>الاسم</th><th>الجهاز</th><th>تحكم</th></tr></thead>
                    <tbody>
                        <% students.forEach(s => { %>
                            <tr>
                                <td><%= s.username %></td>
                                <td><%= s.device_info ? '🔒 مقيد' : '🔓 مفتوح' %></td>
                                <td><a href="/admin/reset/<%= s._id %>" class="btn btn-sm btn-warning">تصفير الجهاز</a></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>
