<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <title>لوحة التحكم الاحترافية - مستر ميدو</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { font-family: 'Cairo', sans-serif; background: #0f172a; color: white; }
        .sidebar { width: 260px; background: #1e293b; position: fixed; height: 100vh; padding: 20px; border-left: 1px solid rgba(255,255,255,0.1); z-index: 100; }
        .main { margin-right: 260px; padding: 40px; }
        .card-custom { background: rgba(255,255,255,0.05); border-radius: 20px; padding: 25px; margin-bottom: 30px; border: 1px solid rgba(255,255,255,0.1); }
        .form-control { background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.1); color: white; border-radius: 10px; }
        .btn-copy { cursor: pointer; color: #6366f1; transition: 0.2s; }
        .btn-copy:hover { color: #10b981; transform: scale(1.2); }
        .active-badge { background: #6366f1; color: white; padding: 2px 12px; border-radius: 50px; font-size: 0.8rem; font-weight: bold; }
        .code-list { max-height: 400px; overflow-y: auto; }
        .code-box { background: rgba(0,0,0,0.2); border-radius: 12px; padding: 12px; margin-bottom: 10px; display: flex; justify-content: space-between; align-items: center; border: 1px solid rgba(255,255,255,0.05); }
    </style>
</head>
<body>
    <div class="sidebar">
        <h2 class="text-primary fw-black text-center mb-5">مستر ميدو</h2>
        <nav class="nav flex-column gap-3">
            <a href="#courses" class="text-white text-decoration-none"><i class="fas fa-play-circle me-2 text-primary"></i> إدارة الكورسات</a>
            <a href="#students" class="text-white text-decoration-none"><i class="fas fa-users me-2 text-info"></i> إدارة الطلاب</a>
            <a href="#codes" class="text-white text-decoration-none"><i class="fas fa-key me-2 text-warning"></i> إدارة الأكواد</a>
            <hr class="opacity-25">
            <a href="/logout" class="btn btn-outline-danger w-100">تسجيل الخروج</a>
        </nav>
    </div>

    <div class="main">
        <h1 class="mb-5 fw-black">Dashboard الإدارة المركزية ⚙️</h1>

        <div class="card-custom" id="courses">
            <h3 class="mb-4 text-primary"><i class="fas fa-video me-2"></i> إضافة كورس جديد</h3>
            <form action="/admin/add-course" method="POST" class="row g-3 mb-4">
                <div class="col-md-4"><input type="text" name="title" class="form-control" placeholder="اسم الكورس" required></div>
                <div class="col-md-4"><input type="text" name="vid" class="form-control" placeholder="YouTube Video ID" required></div>
                <div class="col-md-4"><input type="text" name="thumb" class="form-control" placeholder="رابط صورة الغلاف"></div>
                <div class="col-12"><button type="submit" class="btn btn-primary w-100 fw-bold">نشر الكورس في المنصة</button></div>
            </form>

            <h4 class="mb-3">الكورسات المنشورة حالياً (<%= courses.length %>)</h4>
            <div class="table-responsive">
                <table class="table table-dark table-hover">
                    <thead><tr><th>الغلاف</th><th>العنوان</th><th>الإجراء</th></tr></thead>
                    <tbody>
                        <% courses.forEach(c => { %>
                        <tr>
                            <td><img src="<%= c.thumb %>" width="60" class="rounded shadow-sm"></td>
                            <td><%= c.title %></td>
                            <td><a href="/admin/delete-course/<%= c._id %>" class="btn btn-sm btn-outline-danger" onclick="return confirm('مسح الكورس نهائياً؟')">حذف</a></td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="card-custom" id="students">
            <h3 class="mb-4 text-info"><i class="fas fa-user-graduate me-2"></i> الطلاب المسجلين</h3>
            <table class="table table-dark table-hover text-center">
                <thead><tr><th>الاسم</th><th>البريد</th><th>الكورسات المفعلة</th><th>التحكم</th></tr></thead>
                <tbody>
                    <% students.forEach(s => { %>
                    <tr>
                        <td><%= s.username %></td>
                        <td><small><%= s.email %></small></td>
                        <td><span class="active-badge"><%= s.activeCount %></span></td>
                        <td>
                            <a href="/admin/reset-student/<%= s._id %>" class="btn btn-sm btn-warning fw-bold" onclick="return confirm('تصفير الحساب؟')">تصفير</a>
                            <a href="/admin/delete-student/<%= s._id %>" class="btn btn-sm btn-danger ms-2"><i class="fas fa-trash"></i></a>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>

        <div class="card-custom" id="codes">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h3 class="text-warning"><i class="fas fa-key me-2"></i> الأكواد (اضغط للنسخ)</h3>
                <div>
                    <a href="/admin/generate-keys" class="btn btn-success btn-sm fw-bold">توليد 20 كود</a>
                    <a href="/admin/delete-all-codes" class="btn btn-danger btn-sm fw-bold" onclick="return confirm('مسح كل الأكواد؟')">مسح الكل</a>
                </div>
            </div>
            
            <div class="row code-list">
                <% codes.forEach(c => { %>
                <div class="col-md-4 mb-2">
                    <div class="code-box">
                        <span class="fw-bold"><%= c.code %></span>
                        <div>
                            <i class="fas fa-copy btn-copy me-3" onclick="copyMe('<%= c.code %>', this)"></i>
                            <a href="/admin/delete-code/<%= c._id %>" class="text-danger"><i class="fas fa-times"></i></a>
                        </div>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    </div>

    <script>
        function copyMe(text, icon) {
            navigator.clipboard.writeText(text).then(() => {
                const old = icon.className;
                icon.className = "fas fa-check text-success";
                setTimeout(() => icon.className = old, 1500);
            });
        }
    </script>
</body>
</html>
