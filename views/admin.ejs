    <!-- Alerts -->
    <% if (success) { %>
    <div class="alert alert-success">✅ <%= success %></div>
    <% } %>
    
    <!-- Actions -->
    <div class="actions-row">
        <a href="/admin/generate-keys" class="action-btn btn-gen">🎲 توليد 20 كود جديد</a>
        <a href="/admin/delete-all-codes" class="action-btn btn-del" onclick="return confirm('هل أنت متأكد من مسح كل الأكواد؟')">🗑️ مسح كل الأكواد</a>
    </div>

    <div class="row">
        <!-- Add Course -->
        <div class="col-md-6">
            <div class="card">
                <h4>📚 إضافة كورس جديد</h4>
                <form action="/admin/add-course" method="POST">
                    <input type="text" name="title" class="form-control" placeholder="اسم الكورس" required>
                    <input type="text" name="vid" class="form-control" placeholder="ID فيديو يوتيوب" required>
                    <input type="text" name="thumb" class="form-control" placeholder="رابط الصورة" required>
                    <button type="submit" class="btn-submit">➕ إضافة الكورس</button>
                </form>
            </div>
        </div>

        <!-- Add Code -->
        <div class="col-md-6">
            <div class="card">
                <h4>🔑 إضافة كود تفعيل</h4>
                <form action="/admin/add-code" method="POST">
                    <input type="text" name="newCode" class="form-control" placeholder="أدخل الكود" required>
                    <button type="submit" class="btn-submit">➕ إضافة الكود</button>
                </form>
            </div>
        </div>
    </div>

    <!-- Students Table -->
    <div class="card table-card">
        <h4>👥 الطلاب المسجلين (<%= students.length %>)</h4>
        <table class="table">
            <thead>
                <tr>
                    <th>الاسم</th>
                    <th>الإيميل</th>
                    <th>الكورسات</th>
                    <th>حذف</th>
                </tr>
            </thead>
            <tbody>
                <% students.forEach(s => { %>
                <tr>
                    <td><%= s.username %></td>
                    <td><%= s.email %></td>
                    <td>
                        <% 
                        let enrolled = {};
                        try { enrolled = JSON.parse(s.courses || '{}'); } catch(e) {}
                        %>
                        <span class="badge-custom <%= Object.keys(enrolled).length > 0 ? 'badge-available' : 'badge-used' %>">
                            <%= Object.keys(enrolled).length %> كورس
                        </span>
                    </td>
                    <td>
                        <a href="/admin/delete-student/<%= s._id %>" class="btn-logout" style="padding: 5px 15px; font-size: 0.9rem;" onclick="return confirm('حذف هذا الطالب؟')">🗑️</a>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <!-- Codes -->
    <div class="card">
        <h4>🔐 الأكواد المتوفرة (<%= codes.length %>)</h4>
        <div class="codes-grid">
            <% codes.forEach(c => { %>
            <div class="code-item">
                <span class="<%= c.used ? 'badge-used' : 'badge-available' %>"><%= c.code %></span>
                <a href="/admin/delete-code/<%= c._id %>" class="delete">✕</a>
            </div>
            <% }) %>
        </div>
        <% if (codes.length === 0) { %>
        <p style="color: rgba(255,255,255,0.5); text-align: center; margin-top: 20px;">لا توجد أكواد متوفرة</p>
        <% } %>
    </div>
</div>
