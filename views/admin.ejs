<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة تحكم الأدمن 🛠️</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Cairo', sans-serif; background: #f8f9fa; padding: 20px; }
        .card { border-radius: 15px; border: none; }
    </style>
</head>
<body>
    <div class="container bg-white p-4 shadow rounded-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="text-primary fw-bold">لوحة تحكم مستر ميدو 🛠️</h2>
            <a href="/logout" class="btn btn-danger btn-sm">تسجيل خروج</a>
        </div>

        <!-- رسائل النجاح والخطأ -->
        <% if (success) { %>
        <div class="alert alert-success text-center"><%= success %></div>
        <% } %>
        <% if (error) { %>
        <div class="alert alert-danger text-center"><%= error %></div>
        <% } %>

        <div class="row">
            <div class="col-md-7">
                <div class="card p-3 mb-4 shadow-sm border-primary">
                    <h5 class="fw-bold">إضافة كورس جديد 🎥</h5>
                    <form action="/admin/add-course" method="POST" class="row g-2">
                        <div class="col-md-6"><input type="text" name="title" class="form-control" placeholder="اسم الكورس" required></div>
                        <div class="col-md-6"><input type="text" name="vid" class="form-control" placeholder="ID يوتيوب (مثال: dQw4w9W)" required></div>
                        <div class="col-md-10"><input type="text" name="thumb" class="form-control" placeholder="رابط صورة الغلاف" required></div>
                        <div class="col-md-2"><button type="submit" class="btn btn-primary w-100">إضافة</button></div>
                    </form>
                </div>
            </div>

            <div class="col-md-5">
                <div class="card p-3 mb-4 shadow-sm border-warning">
                    <h5 class="fw-bold">توليد كود تفعيل 🔑</h5>
                    <form action="/admin/add-code" method="POST" class="d-flex gap-2">
                        <input type="text" name="newCode" class="form-control" placeholder="اكتب الكود الجديد هنا" required>
                        <input type="text" name="courseId" class="form-control" placeholder="معرف الكورس (c1, c2)" required>
                        <button type="submit" class="btn btn-warning">حفظ الكود</button>
                    </form>
                </div>
            </div>
        </div>

        <hr>

        <h5 class="fw-bold mt-4">إدارة الطلاب والبيانات 👥</h5>
        <div class="table-responsive">
            <table class="table table-hover align-middle mt-3">
                <thead class="table-dark">
                    <tr>
                        <th>الاسم</th>
                        <th>الإيميل</th>
                        <th>الكورسات المفعلة</th>
                        <th>الجهاز</th>
                        <th>الإجراءات</th>
                    </tr>
                </thead>
                <tbody>
                    <% students.forEach(s => { %>
                    <tr>
                        <td><%= s.username %></td>
                        <td><%= s.email %></td>
                        <td>
                            <span class="badge <%= s.enrolled_courses.length > 0 ? 'bg-success' : 'bg-secondary' %>">
                                <%= s.enrolled_courses.length %> كورس
                            </span>
                        </td>
                        <td><%= s.device_info ? '✅ مربوط' : '⚠️ متاح' %></td>
                        <td>
                            <a href="/admin/reset-student/<%= s._id %>" class="btn btn-outline-warning btn-sm">تصفير الجهاز</a>
                            <a href="/admin/delete-student/<%= s._id %>" class="btn btn-outline-danger btn-sm" onclick="return confirm('هل أنت متأكد من الحذف؟')">حذف</a>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>

        <h5 class="fw-bold mt-5">الأكواد المتوفرة في القاعدة 🔑</h5>
        <div class="d-flex flex-wrap gap-2">
            <% codes.forEach(c => { %>
                <span class="badge <%= c.is_used ? 'bg-secondary text-decoration-line-through' : 'bg-info' %> p-2">
                    <%= c.code %>
                </span>
            <% }) %>
        </div>
    </div>
</body>
</html>
