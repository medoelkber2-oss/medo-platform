<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <title>لوحة التحكم المطورة - مستر ميدو</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { font-family: 'Cairo', sans-serif; background: #0f172a; color: white; }
        .sidebar { width: 260px; background: #1e293b; position: fixed; height: 100vh; padding: 20px; border-left: 1px solid rgba(255,255,255,0.1); }
        .main { margin-right: 260px; padding: 40px; }
        .card-custom { background: rgba(255,255,255,0.05); border-radius: 20px; padding: 25px; margin-bottom: 30px; border: 1px solid rgba(255,255,255,0.1); }
        .btn-copy { cursor: pointer; color: #6366f1; transition: 0.2s; }
        .btn-copy:hover { color: #10b981; transform: scale(1.2); }
        .code-box { background: rgba(0,0,0,0.2); padding: 10px; border-radius: 12px; display: flex; justify-content: space-between; align-items: center; margin-bottom: 10px; }
        .active-badge { background: #6366f1; color: white; padding: 3px 12px; border-radius: 50px; font-size: 0.8rem; }
    </style>
</head>
<body>
    <div class="sidebar">
        <h2 class="text-primary fw-black text-center mb-5">مستر ميدو</h2>
        <nav class="nav flex-column gap-3">
            <a href="#students" class="text-white text-decoration-none"><i class="fas fa-users me-2"></i> إدارة الطلاب</a>
            <a href="#codes" class="text-white text-decoration-none"><i class="fas fa-key me-2"></i> الأكواد والنسخ</a>
            <a href="/logout" class="btn btn-danger w-100 mt-5">خروج</a>
        </nav>
    </div>

    <div class="main">
        <div class="card-custom" id="students">
            <h3>قائمة الطلاب (<%= students.length %>)</h3>
            <table class="table table-dark table-hover mt-3">
                <thead>
                    <tr>
                        <th>الاسم</th>
                        <th>الكورسات المفعلة</th>
                        <th>التحكم</th>
                    </tr>
                </thead>
                <tbody>
                    <% students.forEach(s => { %>
                    <tr>
                        <td><%= s.username %></td>
                        <td><span class="active-badge"><%= s.activeCount %> كورس</span></td>
                        <td>
                            <a href="/admin/reset-student/<%= s._id %>" class="btn btn-sm btn-warning" onclick="return confirm('تصفير حساب الطالب؟ سيطلب منه تفعيل الكورسات مجدداً.')">تصفير الحساب</a>
                            <a href="/admin/delete-student/<%= s._id %>" class="btn btn-sm btn-danger">حذف</a>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>

        <div class="card-custom" id="codes">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h3>الأكواد المنشأة (<%= codes.length %>)</h3>
                <div>
                    <a href="/admin/generate-keys" class="btn btn-primary btn-sm">توليد 20 كود</a>
                    <a href="/admin/delete-all-codes" class="btn btn-danger btn-sm" onclick="return confirm('حذف جميع الأكواد نهائياً؟')">مسح كل الأكواد</a>
                </div>
            </div>
            
            <div class="row">
                <% codes.forEach(c => { %>
                <div class="col-md-4">
                    <div class="code-box">
                        <span class="fw-bold"><%= c.code %></span>
                        <div>
                            <i class="fas fa-copy btn-copy me-2" onclick="copyCode('<%= c.code %>', this)" title="نسخ الكود"></i>
                            <a href="/admin/delete-code/<%= c._id %>" class="text-danger"><i class="fas fa-trash-alt"></i></a>
                        </div>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    </div>

    <script>
        function copyCode(text, icon) {
            navigator.clipboard.writeText(text).then(() => {
                const originalClass = icon.className;
                icon.className = "fas fa-check text-success me-2"; // تغيير الأيقونة لصح أخضر
                setTimeout(() => { icon.className = originalClass; }, 1500);
            });
        }
    </script>
</body>
</html>
