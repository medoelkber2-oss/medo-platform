<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة الأدمن</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
</head>
<body style="background:#f8f9fa;padding:20px">
    <div class="container bg-white p-4 shadow rounded">
        <div class="d-flex justify-content-between mb-4">
            <h2 class="text-primary">لوحة تحكم مستر ميدو</h2>
            <a href="/logout" class="btn btn-danger">تسجيل خروج</a>
        </div>

        <div class="row mb-4">
            <div class="col-md-6">
                <a href="/admin/generate-keys" class="btn btn-success w-100">🎲 توليد 20 كود</a>
            </div>
            <div class="col-md-6">
                <a href="/admin/delete-all-codes" class="btn btn-danger w-100" onclick="return confirm('مسح كل الأكواد؟')">🗑️ مسح كل الأكواد</a>
            </div>
        </div>

        <div class="row">
            <div class="col-md-6">
                <div class="card p-3 mb-4">
                    <h5>إضافة كورس</h5>
                    <form action="/admin/add-course" method="POST">
                        <input type="text" name="title" class="form-control mb-2" placeholder="اسم الكورس" required>
                        <input type="text" name="vid" class="form-control mb-2" placeholder="ID يوتيوب" required>
                        <input type="text" name="thumb" class="form-control mb-2" placeholder="رابط الصورة" required>
                        <button class="btn btn-primary w-100">إضافة</button>
                    </form>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card p-3 mb-4">
                    <h5>إضافة كود</h5>
                    <form action="/admin/add-code" method="POST">
                        <input type="text" name="newCode" class="form-control mb-2" placeholder="الكود" required>
                        <button class="btn btn-warning w-100">إضافة</button>
                    </form>
                </div>
            </div>
        </div>

        <h4>الطلاب</h4>
        <table class="table table-bordered">
            <thead class="table-dark">
                <tr><th>الاسم</th><th>الإيميل</th><th>الكورسات</th><th>حذف</th></tr>
            </thead>
            <tbody>
                <% students.forEach(s => { %>
                <tr>
                    <td><%= s.username %></td>
                    <td><%= s.email %></td>
                    <td><%= Object.keys(JSON.parse(s.c
