<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>منصتي</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
</head>
<body style="background:#f4f7f6">
    <nav class="navbar navbar-dark bg-dark p-3">
        <div class="container d-flex justify-content-between">
            <h4 class="mb-0">أكاديمية مستر ميدو</h4>
            <div>
                <span class="me-3">أهلاً، <strong><%= username %></strong></span>
                <a href="/logout" class="btn btn-outline-danger btn-sm">خروج</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <% if (success) { %>
        <div class="alert alert-success text-center"><%= success %></div>
        <% } %>
        <% if (error) { %>
        <div class="alert alert-danger text-center"><%= error %></div>
        <% } %>

        <h4 class="mb-4">الكورسات المتاحة</h4>
        
        <div class="row">
            <% courses.forEach(course => { %>
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <div class="card-body">
                            <h5><%= course.title %></h5>
                            
                            <% 
                            const data = enrolledList[course.id];
                            const isEnrolled = data && data.views !== undefined;
                            const sameDevice = !data || data.device === sessionId;
                            const remaining = isEnrolled ? (data.max - data.views) : 0;
                            %>
                            
                            <% if (isEnrolled && sameDevice && remaining > 0) { %>
                                <div class="ratio ratio-16x9 mb-2">
                                    <iframe src="https://www.youtube.com/embed/<%= course.vid %>" allowfullscreen></iframe>
                                </div>
                                <p class="text-info">بقي <%= remaining %> مشاهدات</p>
                                
                            <% } else if (isEnrolled && sameDevice && remaining <= 0) { %>
                                <div class="alert alert-warning">انتهت المشاهدات!</div>
                                <form action="/activate/<%= course.id %>" method="POST">
                                    <input type="text" name="activationCode" class="form-control mb-2" placeholder="كود التفعيل" required>
                                    <button class="btn btn-primary w-100">تفعيل</button>
                                </form>
                                
                            <% } else if (isEnrolled && !sameDevice) { %>
                                <div class="alert alert-danger">مرتبط بجهاز آخر!</div>
                                <form action="/activate/<%= course.id %>" method="POST">
                                    <input type="text" name="activationCode" class="form-control mb-2" placeholder="كود التفعيل" required>
                                    <button class="btn btn-primary w-100">تفعيل على جهازي</button>
                                </form>
                                
                            <% } else { %>
                                <img src="<%= course.thumb %>" class="img-fluid rounded mb-2" style="height:150px;object-fit:cover">
                                <form action="/activate/<%= course.id %>" method="POST">
                                    <input type="text" name="activationCode" class="form-control mb-2" placeholder="كود التفعيل" required>
                                    <button class="btn btn-primary w-100">تفعيل الكورس</button>
                                </form>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
