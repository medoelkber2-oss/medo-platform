<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>منصتي - مستر ميدو</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { font-family: 'Cairo', sans-serif; background: #0f172a; color: white; }
        .navbar { background: rgba(30, 41, 59, 0.8); backdrop-filter: blur(10px); border-bottom: 1px solid rgba(255,255,255,0.1); }
        .course-card { 
            background: rgba(255,255,255,0.05); border-radius: 20px; overflow: hidden; 
            border: 1px solid rgba(255,255,255,0.1); transition: 0.3s; margin-bottom: 30px;
        }
        .course-card:hover { transform: translateY(-10px); background: rgba(255,255,255,0.08); }
        .course-thumb { width: 100%; height: 200px; object-fit: cover; }
        .course-body { padding: 20px; text-align: center; }
        .btn-activate { background: #6366f1; border: none; width: 100%; padding: 12px; border-radius: 12px; font-weight: bold; margin-top: 10px; color: white; }
        .btn-watch { background: #10b981; color: white; text-decoration: none; display: block; padding: 12px; border-radius: 12px; font-weight: bold; }
        .status-badge { font-size: 0.8rem; padding: 4px 12px; border-radius: 20px; margin-bottom: 10px; display: inline-block; }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark py-3 sticky-top">
        <div class="container d-flex justify-content-between align-items-center">
            <a class="navbar-brand fw-bold fs-3" href="#">🎓 مستر ميدو</a>
            <div class="d-flex align-items-center gap-3">
                <span>مرحباً، <%= username %></span>
                <a href="/logout" class="btn btn-sm btn-outline-danger">خروج</a>
            </div>
        </div>
    </nav>

    <div class="container mt-5">
        <h2 class="mb-5 fw-bold"><i class="fas fa-book-open me-2 text-primary"></i> الكورسات التعليمية المتاحة</h2>

        <% if (error) { %>
            <div class="alert alert-danger rounded-4 shadow-sm mb-4"><%= error %></div>
        <% } %>

        <div class="row">
            <% if (courses.length > 0) { %>
                <% courses.forEach(course => { %>
                    <div class="col-md-4">
                        <div class="course-card">
                            <img src="<%= course.thumb %>" class="course-thumb" alt="thumbnail">
                            <div class="course-body">
                                <h4 class="mb-3"><%= course.title %></h4>
                                
                                <% if (enrolledList[course._id]) { %>
                                    <div class="status-badge bg-success-subtle text-success border border-success">مفعل ✅</div>
                                    <a href="/video/<%= course._id %>" class="btn-watch shadow">
                                        <i class="fas fa-play me-2"></i> مشاهدة المحتوى
                                    </a>
                                <% } else { %>
                                    <div class="status-badge bg-danger-subtle text-danger border border-danger">غير مفعل 🔒</div>
                                    <form action="/activate/<%= course._id %>" method="POST">
                                        <input type="text" name="activationCode" class="form-control mb-2 text-center" placeholder="أدخل كود التفعيل" required>
                                        <button type="submit" class="btn-activate shadow">تفعيل الكورس</button>
                                    </form>
                                <% } %>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-12 text-center py-5">
                    <p class="text-secondary fs-4">لا توجد كورسات مضافة حالياً..</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
