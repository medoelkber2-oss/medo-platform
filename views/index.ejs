<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <title>منصتي</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { font-family: 'Cairo', sans-serif; background: #0f172a; color: white; }
        .course-card { background: rgba(255,255,255,0.05); border-radius: 20px; overflow: hidden; margin-bottom: 20px; border: 1px solid rgba(255,255,255,0.1); }
        .course-thumb { width: 100%; height: 180px; object-fit: cover; }
    </style>
</head>
<body>
    <div class="container py-5">
        
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="mb-0">أهلاً يا <%= username %> 👋</h2>
            <a href="/logout" class="btn btn-outline-danger shadow-sm">
                <i class="fas fa-sign-out-alt me-1"></i> تسجيل خروج
            </a>
        </div>

        <% if (error) { %>
            <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <div class="row mt-4">
            <% courses.forEach(course => { %>
            <div class="col-md-4">
                <div class="course-card text-center p-3">
                    <img src="<%= course.thumb %>" class="course-thumb rounded mb-3">
                    <h5><%= course.title %></h5>
                    <% if (enrolledList[course._id]) { %>
                        <a href="/video/<%= course._id %>" class="btn btn-success w-100">مشاهدة الكورس</a>
                    <% } else { %>
                        <form action="/activate/<%= course._id %>" method="POST">
                            <input type="text" name="activationCode" class="form-control mb-2" placeholder="كود التفعيل" required>
                            <button class="btn btn-primary w-100">تفعيل الكورس</button>
                        </form>
                    <% } %>
                </div>
            </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
