<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>منصتي | الرئيسية 🎓</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { background: #f8f9fa; font-family: 'Cairo', sans-serif; }
        .hero-section { background: linear-gradient(135deg, #1e293b, #4361ee); color: white; padding: 40px 0; border-radius: 0 0 30px 30px; margin-bottom: 40px; }
        .course-card { border: none; border-radius: 20px; transition: all 0.3s; overflow: hidden; box-shadow: 0 4px 15px rgba(0,0,0,0.05); }
        .course-card:hover { transform: translateY(-10px); box-shadow: 0 12px 25px rgba(0,0,0,0.1); }
        .course-img { height: 180px; object-fit: cover; }
        .btn-enter { background: #4361ee; border: none; border-radius: 10px; font-weight: bold; }
        .logout-btn { background: rgba(255,255,255,0.2); color: white; border: 1px solid white; border-radius: 10px; padding: 5px 15px; text-decoration: none; }
    </style>
</head>
<body>

<div class="hero-section shadow">
    <div class="container d-flex justify-content-between align-items-center">
        <div>
            <h2 class="fw-bold m-0">أهلاً، <%= user.username %> 👋</h2>
            <p class="m-0 opacity-75">جاهز لتكملة رحلة التعلم اليوم؟</p>
        </div>
        <a href="/logout" class="logout-btn"><i class="fas fa-sign-out-alt"></i> خروج</a>
    </div>
</div>

<div class="container">
    <h4 class="fw-bold mb-4"><i class="fas fa-graduation-cap text-primary"></i> كورساتك المتاحة</h4>
    <div class="row g-4">
        <% courses.forEach(course => { %>
            <div class="col-md-4">
                <div class="card course-card h-100">
                    <img src="<%= course.thumb %>" class="course-img" alt="course">
                    <div class="card-body">
                        <h5 class="card-title fw-bold"><%= course.title %></h5>
                        <p class="text-muted small"><i class="far fa-play-circle text-primary"></i> <%= course.lessons %> درس تعليمي</p>
                        <a href="#" class="btn btn-enter btn-primary w-100">ابدأ المشاهدة الآن</a>
                    </div>
                </div>
            </div>
        <% }) %>
    </div>
</div>

</body>
</html>
