<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <title>أكاديمية ميدو 🎓</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <style>
        body { background: #f4f7f6; font-family: 'Cairo', sans-serif; }
        .course-card { border-radius: 15px; overflow: hidden; border: none; transition: 0.3s; }
        .whatsapp-float { position: fixed; bottom: 20px; left: 20px; background: #25d366; color: white; width: 60px; height: 60px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 30px; text-decoration: none; }
    </style>
</head>
<body>

<nav class="navbar navbar-dark bg-dark mb-4 p-3">
    <div class="container d-flex justify-content-between">
        <span class="navbar-brand">مرحباً <%= user.username %> 🎓</span>
        <a href="/logout" class="btn btn-outline-danger">خروج</a>
    </div>
</nav>

<div class="container">
    <div class="row g-4">
        <% courses.forEach(course => { %>
            <div class="col-md-6">
                <div class="card course-card shadow-sm">
                    <img src="<%= course.thumb %>" class="card-img-top" style="height: 200px; object-fit: cover;">
                    <div class="card-body">
                        <h5><%= course.title %></h5>
                        <% if (user.enrolled_courses.includes(course.id)) { %>
                            <% if (deviceMatch) { %>
                                <div class="list-group mt-2">
                                    <% course.lessons.forEach(l => { %>
                                        <button class="list-group-item list-group-item-action" onclick="showVideo('<%= l.vid %>')">▶️ <%= l.title %></button>
                                    <% }) %>
                                </div>
                            <% } else { %>
                                <div class="alert alert-danger mt-2">⚠️ هذا الكورس مقفل على جهازك الأساسي فقط.</div>
                            <% } %>
                        <% } else { %>
                            <form action="/activate/<%= course.id %>" method="POST" class="mt-2">
                                <input type="text" name="activationCode" class="form-control mb-2" placeholder="ادخل كود التفعيل">
                                <button class="btn btn-primary w-100">تفعيل الآن</button>
                            </form>
                        <% } %>
                    </div>
                </div>
            </div>
        <% }) %>
    </div>
</div>

<a href="https://wa.me/20123456789" class="whatsapp-float" target="_blank">📞</a>

<script>
    // كود معالجة التنبيهات
    const params = new URLSearchParams(window.location.search);
    if(params.has('error')) {
        Swal.fire({ icon: 'error', title: 'عذراً', text: params.get('error'), confirmButtonText: 'تمام' });
    }
    if(params.has('success')) {
        Swal.fire({ icon: 'success', title: 'تم بنجاح', text: params.get('success'), timer: 2000 });
    }
    
    function showVideo(id) {
        Swal.fire({
            html: `<iframe width="100%" height="315" src="https://www.youtube.com/embed/${id}" frameborder="0" allowfullscreen></iframe>`,
            showCloseButton: true, showConfirmButton: false, width: 800
        });
    }
</script>
</body>
</html>
