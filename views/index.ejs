<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo&display=swap" rel="stylesheet">
    <style>body { background: #f4f7fe; font-family: 'Cairo'; }</style>
</head>
<body>
    <div class="p-4 bg-dark text-white d-flex justify-content-between mb-4 shadow">
        <h4>أهلاً، <%= user.username %> 👋</h4>
        <a href="/logout" class="btn btn-outline-light btn-sm">تسجيل خروج</a>
    </div>

    <div class="container">
        <% if (error) { %><div class="alert alert-danger text-center shadow-sm"><%= error %></div><% } %>

        <% if (!user.is_active && user.role !== 'admin') { %>
            <div class="card p-5 text-center border-warning shadow-sm mb-4">
                <h3 class="fw-bold text-warning mb-3">⚠️ المنصة غير مفعلة</h3>
                <p>يرجى إدخال كود التفعيل لتتمكن من مشاهدة الفيديوهات</p>
                <form action="/activate" method="POST" class="d-inline-flex gap-2">
                    <input type="text" name="code" class="form-control" placeholder="أدخل الكود هنا" required>
                    <button class="btn btn-primary">تفعيل الآن</button>
                </form>
            </div>
        <% } %>

        <div class="row g-4">
            <% courses.forEach(c => { %>
                <div class="col-md-4">
                    <div class="card h-100 shadow-sm border-0" style="border-radius: 15px; overflow:hidden;">
                        <img src="<%= c.thumb %>" class="card-img-top">
                        <div class="card-body text-center">
                            <h5 class="fw-bold"><%= c.title %></h5>
                            <a href="/course/<%= c.id %>" class="btn btn-primary w-100 mt-2">مشاهدة الدرس</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
