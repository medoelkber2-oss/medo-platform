<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>أكاديميتي 🎓 - لوحة الطالب</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Cairo', sans-serif; background: #f8f9fa; }
        .course-card { border: none; border-radius: 15px; background: white; transition: 0.3s; }
        .course-card:hover { transform: translateY(-5px); }
        .video-container { position: relative; padding-bottom: 56.25%; height: 0; background: #000; border-radius: 15px; overflow: hidden; }
        .video-container iframe { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }
        .navbar { background: #212529; color: white; }
        .locked-msg { background: #fff3f3; color: #d9534f; padding: 15px; border-radius: 10px; border: 1px solid #ffcccc; }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg p-3 mb-5 shadow">
    <div class="container d-flex justify-content-between align-items-center">
        <h3 class="mb-0">أكاديميتي 🎓</h3>
        <div class="d-flex align-items-center">
            <span class="me-3">أهلاً، <strong><%= username %></strong></span>
            <a href="/logout" class="btn btn-sm btn-outline-danger">تسجيل خروج</a>
        </div>
    </div>
</nav>

<div class="container">
    <div class="row g-4">
        <% courses.forEach(course => { %>
            <div class="col-md-6 col-lg-4">
                <div class="card course-card shadow-sm h-100">
                    <div class="card-body">
                        <h5 class="fw-bold mb-3"><%= course.title %></h5>

                        <% if (enrolledList.includes(course.id)) { %>
                            <% if (deviceMatch) { %>
                                <div class="video-container shadow-sm">
                                    <iframe src="https://www.youtube.com/embed/<%= course.vid %>" frameborder="0" allowfullscreen></iframe>
                                </div>
                                <p class="text-success mt-3 text-center fw-bold">تم التفعيل ✅ (مشاهدة ممتعة)</p>
                            <% } else { %>
                                <div class="locked-msg text-center mt-3">
                                    <p class="fw-bold mb-1">⚠️ خطأ في الوصول</p>
                                    <small>هذا الكورس مفعل على جهاز آخر. لا يمكنك المشاهدة إلا من جهازك الأصلي.</small>
                                </div>
                            <% } %>

                        <% } else { %>
                            <img src="<%= course.thumb %>" class="img-fluid rounded mb-3" style="height: 180px; width: 100%; object-fit: cover;">
                            
                            <form action="/activate/<%= course.id %>" method="POST">
                                <div class="input-group mb-2">
                                    <input type="text" name="activationCode" class="form-control" placeholder="ادخل كود التفعيل" required>
                                </div>
                                <button type="submit" class="btn btn-primary w-100 rounded-pill">تفعيل الكورس</button>
                            </form>
                            <p class="text-muted small text-center mt-2">للحصول على الكود تواصل معنا عبر الواتساب</p>
                        <% } %>

                    </div>
                </div>
            </div>
        <% }) %>
    </div>
</div>

<footer class="text-center mt-5 mb-4 text-muted small">
    &copy; 2026 جميع الحقوق محفوظة لـ <%= username %> Academy
</footer>

</body>
</html>
