<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>أكاديمية ميدو 🎓 | أفضل الكورسات الحصرية</title>
    <meta name="description" content="منصة ميدو لتعليم البرمجة والتسويق الرقمي بأحدث الطرق.">
    <meta name="keywords" content="برمجة, تسويق, ميدو الكبير, تعلم البرمجة من الصفر">
    <meta property="og:title" content="أكاديمية ميدو 🎓">
    <meta property="og:image" content="https://images.unsplash.com/photo-1498050108023-c5249f4df085?w=500">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Cairo', sans-serif; background: #f4f7f6; }
        .course-card { border-radius: 15px; border: none; overflow: hidden; }
        .video-container { position: relative; padding-bottom: 56.25%; height: 0; background: #000; border-radius: 10px; overflow: hidden; }
        .video-container iframe { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }
        .locked-badge { background: #fee; color: #c33; padding: 10px; border-radius: 8px; border: 1px solid #fcc; font-size: 0.9rem; }
    </style>
</head>
<body>

<nav class="navbar navbar-dark bg-dark shadow-sm p-3 mb-5">
    <div class="container d-flex justify-content-between">
        <span class="navbar-brand mb-0 h1">أكاديمية ميدو 🎓</span>
        <div class="text-white">
            <small class="me-3">مرحباً، <%= username %></small>
            <a href="/logout" class="btn btn-outline-danger btn-sm">خروج</a>
        </div>
    </div>
</nav>

<div class="container">
    <div class="row g-4">
        <% courses.forEach(course => { %>
            <div class="col-md-6 col-lg-4">
                <div class="card course-card shadow-sm">
                    <div class="card-body">
                        <h5 class="fw-bold"><%= course.title %></h5>
                        <hr>

                        <% if (enrolledList.includes(course.id)) { %>
                            <% if (deviceMatch) { %>
                                <div class="video-container">
                                    <iframe src="https://www.youtube.com/embed/<%= course.vid %>" frameborder="0" allowfullscreen></iframe>
                                </div>
                                <div class="alert alert-success mt-2 p-1 text-center small">الكورس مفعل ✅</div>
                            <% } else { %>
                                <div class="locked-badge text-center">
                                    <strong>⚠️ حماية الجهاز</strong><br>
                                    هذا الكورس مقفل على الجهاز الذي فعلته منه أول مرة.
                                </div>
                            <% } %>
                        <% } else { %>
                            <img src="<%= course.thumb %>" class="img-fluid rounded mb-3" style="height: 160px; width: 100%; object-fit: cover;">
                            <form action="/activate/<%= course.id %>" method="POST">
                                <input type="text" name="activationCode" class="form-control mb-2" placeholder="ادخل كود التفعيل" required>
                                <button type="submit" class="btn btn-primary w-100">تفعيل الآن</button>
                            </form>
                        <% } %>
                    </div>
                </div>
            </div>
        <% }) %>
    </div>
</div>

<footer class="text-center p-5 text-muted">
    &copy; 2026 Mido Academy - جميع الحقوق محفوظة
</footer>

</body>
</html>
