
<html dir="rtl" lang="ar"><head><title>منصتي</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet"><link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
<style> body { font-family: 'Cairo', sans-serif; background: #f8f9fa; } .course-card { border: none; border-radius: 15px; background: white; } .video-container { position: relative; padding-bottom: 56.25%; height: 0; background: #000; border-radius: 15px; overflow: hidden; } .video-container iframe { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }</style></head>
<body>
<nav class="navbar bg-dark text-white p-3 mb-5 shadow">
    <div class="container d-flex justify-content-between"><h3>أكاديميتي 🎓</h3><div><span>أهلاً <%= username %></span><a href="/logout" class="btn btn-sm btn-outline-danger ms-3">خروج</a></div></div>
</nav>
<div class="container"><div class="row g-4">
    <% courses.forEach(course => { %>
        <div class="col-md-6 col-lg-4">
            <div class="card course-card shadow-sm h-100"><div class="card-body">
                <h5 class="fw-bold"><%= course.title %></h5>
                <% if (enrolledList.includes(course.id)) { %>
                    <div class="video-container mt-3"><iframe src="https://www.youtube.com/embed/<%= course.vid %>" frameborder="0" allowfullscreen></iframe></div>
                    <p class="text-success mt-2 text-center fw-bold">تم التفعيل ✅</p>
                <% } else { %>
                    <img src="<%= course.thumb %>" class="img-fluid rounded mb-3 mt-2">
                    <form action="/activate/<%= course.id %>" method="POST">
                        <div class="input-group mb-2">
                            <input type="text" name="activationCode" class="form-control" placeholder="ادخل كود التفعيل" required>
                        </div>
                        <button type="submit" class="btn btn-primary w-100 rounded-pill">تفعيل الكورس</button>
                    </form>
                    <p class="text-muted small text-center mt-2">للحصول على الكود تواصل معنا على الواتساب</p>
                <% } %>
            </div></div>
        </div>
    <% }) %>
</div></div></body></html>