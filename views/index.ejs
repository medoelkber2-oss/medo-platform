<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@700&display=swap" rel="stylesheet">
    <style>
        body { background: #f4f7fe; font-family: 'Cairo', sans-serif; }
        .hero { background: linear-gradient(135deg, #1e293b, #4361ee); color: white; padding: 40px; border-radius: 0 0 30px 30px; }
        .card { border: none; border-radius: 20px; transition: 0.3s; box-shadow: 0 10px 20px rgba(0,0,0,0.05); }
        .card:hover { transform: translateY(-10px); }
        .btn-primary { background: #4361ee; border: none; border-radius: 12px; }
    </style>
</head>
<body>
    <div class="hero mb-5 d-flex justify-content-between align-items-center">
        <h2>أهلاً، <%= user.username %> 👋</h2>
        <a href="/logout" class="btn btn-outline-light">تسجيل الخروج</a>
    </div>
    <div class="container">
        <div class="row g-4">
            <% courses.forEach(c => { %>
                <div class="col-md-4">
                    <div class="card h-100">
                        <img src="<%= c.thumb %>" class="card-img-top" style="border-radius: 20px 20px 0 0; height: 180px; object-fit: cover;">
                        <div class="card-body text-center">
                            <h5 class="fw-bold mb-3"><%= c.title %></h5>
                            <a href="/course/<%= c.id %>" class="btn btn-primary w-100 py-2">مشاهدة الكورس الآن</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
