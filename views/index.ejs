<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>منصتي التعليمية 🎓</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Cairo', sans-serif; background: #f4f7f6; }
        .navbar { background: #212529; color: white; }
        .course-card { border: none; border-radius: 15px; overflow: hidden; transition: 0.3s; height: 100%; }
        .course-card:hover { transform: translateY(-5px); }
        .video-container { position: relative; padding-bottom: 56.25%; height: 0; background: #000; border-radius: 10px; overflow: hidden; }
        .video-container iframe { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }
    </style>
</head>
<body>

<nav class="navbar p-3 mb-5 shadow">
    <div class="container d-flex justify-content-between align-items-center">
        <h3 class="mb-0 fw-bold">أكاديمية مستر ميدو 🎓</h3>
        <div class="d-flex align-items-center">
            <span class="me-3">أهلاً، <strong><%= username %></strong></span>
            <a href="/logout" class="btn btn-sm btn-outline-danger">خروج</a>
        </div>
    </div>
</nav>

<div class="container">
    <!-- رسائل النجاح والخطأ -->
    <% if (success) { %>
    <div class="alert alert-success text-center"><%= success %></div>
    <% } %>
    <% if (error) { %>
    <div class="alert alert-danger text-center"><%= error %></div>
    <% } %>

    <h3 class="fw-bold mb-4">📚 كورساتي المتاحة</h3>
    <div class="row g-4">
        <% if (courses.length > 0) { %>
            <% courses.forEach(course => { %>
                <div class="col-md-4">
                    <div class="card course-card shadow-sm">
                        <div class="card-body">
                            <h5 class="fw-bold mb-3"><%= course.title %></h5>
                            
                            <% 
                            const courseData = enrolledList[course.id];
                            const isEnrolled = courseData && courseData.activated;
                            const isSameDevice = courseData && courseData.device_id === deviceId;
                            const remainingViews = isEnrolled ? (courseData.max_views - courseData.views) : 0;
                            %>
                            
                            <% if (isEnrolled && isSameDevice && remainingViews > 0) { %>
                                <!-- الكورس مفعل + نفس الجهاز + فيه مشاهدات -->
                                <div class="video-container shadow-sm">
                                    <iframe src="https://www.youtube.com/embed/<%= course.vid %>" frameborder="0" allowfullscreen></iframe>
                                </div>
                                <p class="text-info mt-2 text-center fw-bold">
                                    📺剩余 <%= remainingViews %> مشاهدات من <%= courseData.max_views %>
                                </p>
                                
                            <% } else if (isEnrolled && isSameDevice && remainingViews <= 0) { %>
                                <!-- نفس الجهاز لكن خلصت المشاهدات -->
                                <div class="alert alert-warning text-center">
                                    ⚠️ انتهت المشاهدات! يرجى تفعيل الكورس بكود جديد
                                </div>
                                <form action="/activate/<%= course.id %>" method="POST">
                                    <div class="input-group mb-2">
                                        <input type="text" name="activationCode" class="form-control" placeholder="ادخل كود التفعيل" required>
                                    </div>
                                    <button type="submit" class="btn btn-primary w-100 rounded-pill">تفعيل الكورس</button>
                                </form>
                                
                            <% } else if (isEnrolled && !isSameDevice) { %>
                                <!-- الكورس مرتبط بجهاز تاني -->
                                <div class="alert alert-danger text-center">
                                    🔒 هذا الكورس مرتبط بجهاز آخر!<br>
                                    يرجى التفعيل من جهازك الأصلي
                                </div>
                                <form action="/activate/<%= course.id %>" method="POST">
                                    <div class="input-group mb-2">
                                        <input type="text" name="activationCode" class="form-control" placeholder="ادخل كود التفعيل" required>
                                    </div>
                                    <button type="submit" class="btn btn-primary w-100 rounded-pill">تفعيل على جهازي</button>
                                </form>
                                
                            <% } else { %>
                                <!-- الكورس غير مفعل -->
                                <img src="<%= course.thumb %>" class="img-fluid rounded mb-3" style="height: 180px; width: 100%; object-fit: cover; opacity: 0.8;">
                                <form action="/activate/<%= course.id %>" method="POST">
                                    <div class="input-group mb-2">
                                        <input type="text" name="activationCode" class="form-control" placeholder="ادخل كود التفعيل" required>
                                    </div>
                                    <button type="submit" class="btn btn-primary w-100 rounded-pill">تفعيل الكورس</button>
                                </form>
                            <% } %>
                            
                        </div>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div
