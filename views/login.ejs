<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تسجيل الدخول | منصة ميدو 🎓</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        body {
            background: linear-gradient(135deg, #f0f2f5 0%, #c3cfe2 100%);
            font-family: 'Cairo', sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }

        .login-card {
            background: #ffffff;
            border: none;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
            width: 100%;
            max-width: 400px;
            padding: 40px 30px;
        }

        .login-header {
            text-align: center;
            margin-bottom: 30px;
        }

        .login-header i {
            font-size: 50px;
            color: #4361ee;
            margin-bottom: 15px;
        }

        .login-header h2 {
            font-weight: 700;
            color: #1e293b;
        }

        .form-control {
            border-radius: 10px;
            padding: 12px 15px;
            border: 1px solid #e2e8f0;
            margin-bottom: 20px;
        }

        .form-control:focus {
            border-color: #4361ee;
            box-shadow: 0 0 0 3px rgba(67, 97, 238, 0.1);
        }

        .btn-login {
            background: linear-gradient(135deg, #4361ee, #3a0ca3);
            border: none;
            border-radius: 10px;
            padding: 12px;
            font-weight: bold;
            color: white;
            width: 100%;
            transition: all 0.3s;
        }

        .btn-login:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(67, 97, 238, 0.3);
            color: white;
        }

        .error-alert {
            background-color: #fee2e2;
            color: #dc2626;
            padding: 10px;
            border-radius: 8px;
            font-size: 0.9rem;
            margin-bottom: 20px;
            text-align: center;
            border: 1px solid #fecaca;
        }

        .forgot-pass {
            text-align: center;
            margin-top: 20px;
        }

        .forgot-pass a {
            color: #64748b;
            text-decoration: none;
            font-size: 0.85rem;
            transition: color 0.3s;
        }

        .forgot-pass a:hover {
            color: #4361ee;
        }

        .whatsapp-icon {
            color: #25d366;
            margin-left: 5px;
        }
    </style>
</head>
<body>

<div class="login-card">
    <div class="login-header">
        <i class="fas fa-user-graduate"></i>
        <h2>تسجيل الدخول</h2>
        <p class="text-muted">مرحباً بك في منصة ميدو التعليمية</p>
    </div>

    <% if (error) { %>
        <div class="error-alert">
            <i class="fas fa-exclamation-circle me-1"></i> <%= error %>
        </div>
    <% } %>

    <form action="/login" method="POST">
        <div class="mb-1">
            <label class="form-label small fw-bold">البريد الإلكتروني</label>
            <input type="email" name="email" class="form-control" placeholder="البريد الالكتروني" required>
        </div>
        
        <div class="mb-1">
            <label class="form-label small fw-bold">كلمة السر</label>
            <input type="password" name="password" class="form-control" placeholder="كلمه السسر" required>
        </div>

        <button type="submit" class="btn btn-login mt-2">
            دخول للمنصة <i class="fas fa-sign-in-alt ms-1"></i>
        </button>
    </form>

    <div class="forgot-pass">
        <a href="/forgot-password">
            <i class="fab fa-whatsapp whatsapp-icon"></i> نسيت كلمة السر؟ تواصل مع الدعم
        </a>
    </div>
</div>

</body>
</html>
