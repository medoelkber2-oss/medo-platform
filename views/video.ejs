<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <title><%= course.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { font-family: 'Cairo', sans-serif; background: #0f172a; color: white; }
        .video-container { position: relative; padding-bottom: 56.25%; height: 0; border-radius: 15px; overflow: hidden; background: #000; }
        .video-container iframe { position: absolute; top:0; left:0; width:100%; height:100%; border:0; }
        .playlist-card { background: rgba(255,255,255,0.05); border-radius: 15px; padding: 20px; height: 100%; }
        .list-group-item { background: transparent; color: white; border: 1px solid rgba(255,255,255,0.1); margin-bottom: 5px; cursor: pointer; }
        .list-group-item.active { background: #6366f1 !important; border-color: #6366f1; }
    </style>
</head>
<body>
    <div class="container py-4">
        <div class="d-flex justify-content-between mb-4">
            <h3><%= course.title %></h3>
            <a href="/home" class="btn btn-outline-light">الرجوع للرئيسية</a>
        </div>

        <div class="row">
            <div class="col-md-8">
                <div class="video-container shadow-lg">
                    <% if(course.lectures && course.lectures.length > 0) { %>
                        <iframe src="https://www.youtube.com/embed/<%= course.lectures[lecIndex].vid %>" allowfullscreen></iframe>
                    <% } else { %>
                        <div class="text-center p-5">لا يوجد محاضرات في هذا الكورس</div>
                    <% } %>
                </div>
                <h4 class="mt-3 text-info"><%= course.lectures[lecIndex]?.title %></h4>
            </div>
            
            <div class="col-md-4">
                <div class="playlist-card">
                    <h5 class="mb-3 text-primary"><i class="fas fa-list-ol"></i> قائمة المحاضرات</h5>
                    <div class="list-group">
                        <% course.lectures.forEach((lec, index) => { %>
                            <a href="/video/<%= course._id %>?lec=<%= index %>" 
                               class="list-group-item list-group-item-action <%= index == lecIndex ? 'active' : '' %>">
                                المحاضرة <%= index + 1 %>: <%= lec.title %>
                            </a>
                        <% }) %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
