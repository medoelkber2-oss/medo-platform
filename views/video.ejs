<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <style>
        body { background: #0f172a; color: white; font-family: 'Cairo', sans-serif; }
        .video-box { max-width: 900px; margin: 40px auto; }
        iframe { width: 100%; aspect-ratio: 16/9; border-radius: 20px; border: 3px solid #4361ee; }
    </style>
</head>
<body>
    <div class="container video-box">
        <a href="/home" class="btn btn-outline-info mb-4"> < العودة للمنصة</a>
        <h2 class="mb-4 fw-bold"><%= course.title %></h2>
        <iframe src="https://www.youtube.com/embed/<%= course.vid %>" allowfullscreen></iframe>
    </div>
</body>
</html>
